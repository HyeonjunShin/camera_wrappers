{
    "name": "Ubuntu (with Dockerfile)",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    }, 
    "runArgs": [
        "--privileged",
		"-it",
        "--device=/dev/bus/usb:/dev/bus/usb",
        "-v", "/tmp/.X11-unix:/tmp/.X11-unix",
        "-v", "${localEnv:HOME}/.Xauthority:/home/vscode/.Xauthority",
        "-e", "DISPLAY=${localEnv:DISPLAY}",
        "--gpus", "all",
        "--ipc", "host",
        "--net", "host"
    ],
    
    // "features": {},
    // "forwardPorts": [],
    "remoteUser": "vscode"
}